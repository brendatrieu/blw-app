import { PlaceholderPage } from "./PlaceholderPage.js";

export function SymptomCheckPage() {
  return (
    <PlaceholderPage
      title="Symptom Check"
      description="Works with or without an AI key. Not medical advice — pattern-spotting only."
    />
  );
}
