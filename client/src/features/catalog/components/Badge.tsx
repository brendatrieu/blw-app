// Canonical implementation lives in the shared UI kit; re-exported here so
// existing feature/page imports don't need to change.
export { Badge, type BadgeTone } from "../../../components/ui/Badge.js";
